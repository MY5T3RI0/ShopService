@page
@model DeliveryCreateModel


<form method="post" id="myForm" enctype="multipart/form-data" novalidate="novalidate">

    <div class="row">

        <label class="col">Product</label>

        <label class="col">Product Count</label>

    </div>
    
    @for(int i = 0; i < 10; i++)
    {
        <div class="row">

            <div class="form-group col row">
                <select name="DeliveryInfos[@i].ProductId" asp-items="@Model.Products" class="form-control col"></select>
            </div>

            <input type="number" name="DeliveryInfos[@i].ProductCount" class="col" value="0">

        </div>
    }       

    <br/>

    <div class="row">

        <div class="form-group col">
            <div>Store</div>
            <select name="storeId" asp-items="@Model.Stores" class="form-control"></select>
        </div>

        <button type="submit" class="btn btn-success col">Create Delivery</button>

    </div>

</form>

@if (Model.ValidationErrors is not null)
{
    <div class="alert alert-danger">
        <ul>
            @foreach (var error in Model.ValidationErrors)
            {
                <li>@error</li>
            }
        </ul>
    </div>
}